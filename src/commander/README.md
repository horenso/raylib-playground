# Commander

Commander is a visual scripting language for tasks you would usually perform using a shell.
Instead of writing shell commands and piping them together, you build a graph by connecting operation nodes.

Connections are strongly typed, which makes data flow easier to understand and helps catch incompatible operations early.
Each node can also preview its input and output, for easy inspection, debugging, and iteration.
